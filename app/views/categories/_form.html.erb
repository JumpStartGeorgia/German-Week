<%= semantic_form_for @category do |f| %>
  	<%= f.inputs do %>
  		<%= f.input :title %>
  		<%= f.hidden_field :locale , :value => I18n.locale %>
  	<% end %>
	<%= f.actions :submit, :cancel %>
<% end %>

