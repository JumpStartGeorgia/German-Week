<%= form_for(@tag_translation) do |f| %>
  <% if @tag_translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_translation.errors.count, "error") %> prohibited this tag_translation from being saved:</h2>

      <ul>
      <% @tag_translation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.number_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :locale %><br />
    <%= f.text_field :locale %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
